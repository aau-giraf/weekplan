import React from "react";
import LoginScreen from "../components/LoginScreen";

const LoginApp: React.FC = () => {
  //maybe logic for logging in and if they are logged in, redirect to the main page
  return <LoginScreen />;
};

export default LoginApp;
